# DouK 全流程一体化管理器 v0.1.3 开发构建

## 版本定位

v0.1.3 是 `develop` 上的下载账号结果汇总开发构建，不创建 Tag 或 Release，也不进入
`main`。`v0.1.0` Tag 和 Release 保持不变。

## 下载账号结果汇总

下载进程退出码与账号结果是两类独立结论。退出码 `0` 只表示下载器进程正常结束，
不表示每个账号都成功获取或下载作品。

每个下载进程结束后的固定顺序是：等待进程退出，后台逐行读取本次原生日志区间，
完成账号状态判定与一致性校验，将汇总追加到既有 `DownloadTask_*.log`，更新管理器
信息框，然后才允许队列启动下一任务。正常退出、异常退出和用户停止都会进入汇总流程。

下载器原生日志保持在原路径，管理器不复制、不修改原生日志。本功能不生成独立缓存、
目录快照、结果 JSON 或其他单独结果文件。

## 状态定义

- **有新作品下载**：日志最终统计的下载作品数大于 0。
- **作品均被引擎跳过**：筛选后有作品，但视频、图集和实况最终全部计入跳过。
- **无符合条件作品**：筛选处理后的作品数量为 0。
- **私密账号**：出现明确的私密账号提示。
- **处理异常，需核对**：已开始账号的主状态；账号块中出现无法确认已恢复的错误。
- **处理中断**：账号已经开始处理，但进程结束前未形成可确认的最终结果。
- **进入处理前异常**：URL/sec_user_id 解析失败而未进入账号处理；不计入已开始账号主状态合计。
- **未开始**：仅指本次任务启动时 `enable=true` 且 URL 有效、但在进程结束前尚未轮到的账号；不计入已开始账号主状态合计，本次 `enable=false` 或 URL 为空的账号不参与统计。
- **完成但有异常记录**：出现网络中断或重试，但之后仍产生完整的作品统计；这是可与主状态重叠的附加状态，不计入主状态合计。
- **结果不完整**：用户停止、异常退出、日志截断、计划账号未全部完成，或其他证据不足导致无法确认完整任务结果。

六类已开始账号主状态互斥，合计必须等于实际开始账号数。界面和任务日志会将
主状态、尚未进入处理的计划账号及附加状态分区显示，避免重复计数。

## 2026-08-13 最终验收记录

最终运行时代码提交 `cb76a90b4df615e7f96809f12d119180a51b2771` 已完成 Windows 人工验收：

- 自动测试共 174 项，172 通过，2 项可选桌面 WScript 测试跳过；
- 测试 1“正常完成单任务”和测试 2“用户主动停止”最终复测通过；
- 测试 3“两任务队列顺序”、测试 4“安全门”和测试 5“日志与文件边界”沿用此前通过结果；
- GitHub Actions Run #42 的 `Test` 与 `Windows portable package` 均成功，构建提交为 `cb76a90`。

主动停止场景的最终修复会保留停止前已可靠确认的账号结果，并将已经开始但未形成最终结果的账号标记为“处理中断”；已开始账号主状态、未进入处理账号和附加状态分区显示，避免重复计数。正常任务、停止后的部分汇总、队列顺序、安全门、原生日志路径、任务日志、A 编号和文件边界均已按上述测试结论验收。

## 安全边界与回滚

本功能不修改下载引擎、正式 Volume、`settings.json`、`settings_master.json` 或
`DouK-Downloader.db`。如需回滚，先停止管理器、采集器和下载引擎，再恢复此前保留的
上一版管理器可执行文件或便携包即可；不需要也不得迁移或回滚 Volume 和数据库。
