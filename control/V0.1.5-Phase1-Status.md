# DouK-Manager V0.1.5 Phase 1 状态

日期：2026-08-18

结论：**FROZEN / NOT ACCEPTED**。Phase 1 代码、专项测试、固定生命周期探针和全量回归均已通过，但普通 Windows GUI 合成验收未完成。按设计强制门禁，禁止进入 Phase 2、Phase 3、版本发布或打包。

## 范围与提交

- 分支：`feature/v0.1.5-phase1-background-core`
- 基线：`6885ba0ca0696f45ec861c13c44e4b92d018b8c6`
- 当前实现 HEAD：`639e2d0172cb20078b583cb06ff9e28845602f64`
- `8a0018a`：Phase 1 启动安全测试
- `9297d38`、`69b8176`：结构化进程探针、启动安全服务和 Controller 门禁
- `aaecec5`、`492d387`：后台任务协调器、唯一终态和 Qt 生命周期修复
- `97696ca`、`7df13f2`、`d9628de`：先显示窗口、启动状态 UI、失败关闭和有界诊断
- `639e2d0`：非阻塞关闭、协作取消、idle 后关闭重试和迟到结果保护

## 环境身份

- PowerShell `whoami`：`desktop-okjvph7\codexsandboxoffline`
- Python 子进程实际 `whoami`：`desktop-okjvph7\codexsandboxoffline`
- 仓库与 `.git` 所有者：`DESKTOP-OKJVPH7\CodexSandboxOffline`
- Python `getpass.getuser()`/`USERNAME` 显示 `TUF`，但这是继承环境变量，不代表进程安全令牌。
- 未修改 ACL、所有权、Git `safe.directory`、PATH、注册表或全局 Git 配置。

## 已通过门禁

1. 最终专项测试：
   - 命令：`python -m unittest tests.test_startup_safety tests.test_controller_runtime_safety tests.test_background_tasks tests.test_startup_gui tests.test_gui_worker -v`
   - 结果：`Ran 103 tests in 6.741s`，`OK`。
2. 固定 Qt 生命周期子进程探针：
   - 命令：`python tests\qt_lifecycle_probe.py`
   - 结果：`qt lifecycle probe: 20/20 rounds passed`，退出码 0。
   - 未出现 `QThread: Destroyed while thread is still running`、deleted-C++-object 或原生崩溃诊断。
3. 全量测试仅运行一次：
   - 命令：`python -m unittest discover -s tests -v`
   - 结果：`Ran 259 tests in 15.915s`，`OK (skipped=2)`。
   - 两项跳过均为既有可选桌面 WScript 集成测试。
4. 编译检查：
   - 沙箱内首次执行因禁止 Python 在源码树创建 `__pycache__` 而失败。
   - ACL 只读检查确认仓库、`src`、`tests` 均由 `CodexSandboxOffline` 所有，且当前组具有 `Modify`；这不是 dubious ownership。
   - 经批准仅将同一条 `python -m compileall -q src tests` 在沙箱外重跑一次，退出码 0。
5. Git 检查：
   - `git diff --check` 通过。
   - `git diff 6885ba0..HEAD --check` 通过。
   - 写入本状态文件前工作树干净。

## 合成 GUI 验收

验收根目录：`D:\AI-Projects\DouK-Manager-V0.1.5-Codex\local-runtime\acceptance-data`

仅创建并访问以下合成内容：假的 `main.exe`、合成 `settings_master.json`/`settings.json`、合成 SQLite 数据库、合成日志/备份/截图目录。未访问 `F:` 正式路径，未启动下载器或采集器。

已确认：

- 窗口在启动检查开始前可见。
- 启动检查阻塞期间，GUI heartbeat、移动、缩放和标签页切换仍继续。
- 合成结果可进入 `READY`。
- 生成了 `01-safety-checking.png` 和 `02-ready.png`；窗口非空且布局未发现控件重叠。

未完成：

- 缺失路径的端到端 `DEGRADED_READ_ONLY`。
- 技术详情、复制错误、打开日志和重查的同一 GUI 验收链路。
- 不重启修复路径后恢复 `READY`。
- PowerShell `UNKNOWN` 场景后的 GUI 重查链路。
- 关闭/完成竞态的端到端 GUI 验收。

第一次验收脚本因错误假设“READY 后所有危险控件均启用”停止；该假设违反“READY 仍受 V0.1.4 运行态门禁”的既定合同，已仅修正验收脚本。

第二次验收在尝试从 READY 保存缺失 `main.exe` 路径时停止。只读诊断确认实际进程探针返回：

```text
UNKNOWN
Get-CimInstance Win32_Process ... PermissionDenied
HRESULT 0x80041003
```

`UNKNOWN` 按设计必须 fail-closed；`reconfigure()` 在保存前拒绝切换路径，合成 `app_config.json` 保持原值。达到本轮验收重试上限后未再运行第三次验收。

## 剩余风险与下一门禁

- 当前沙箱无法证明普通交互式 Windows 桌面会话中的完整 GUI 验收。
- offscreen 截图环境缺少 Windows 中文字体，文字显示为方框；这不证明真实 Windows 桌面字体渲染。
- 自动测试已覆盖缺失路径、UNKNOWN、复制/日志、重查、迟到结果和关闭竞态，但不能替代强制的真实 GUI 验收。
- 继续工作前需要新的明确批准和一个可正常执行 `Get-CimInstance Win32_Process` 的普通 Windows GUI 会话；不得通过修改 ACL、所有权、`safe.directory` 或弱化 UNKNOWN 门禁来绕过。

## 明确未执行

- 未打包、未构建发布物、未修改版本号/CI/发布说明。
- 未 push、未创建 PR、未合并分支。
- 未访问正式数据，未启动真实下载器/采集器。
- 未进入 Phase 2 或 Phase 3。
