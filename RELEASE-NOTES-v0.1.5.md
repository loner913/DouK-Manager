# DouK 全流程一体化管理器 v0.1.5

## 版本定位

v0.1.5 将后台任务安全、共享操作协调、独立结果看板和两项最终稳定化修复整合到一条经过冻结验收的
线性历史中。最终身份分支直接继承 `release/v0.1.5-stabilization` 的永久冻结 HEAD；本阶段只同步
`feature/v0.1.5-final` 和 `develop`，不创建 Tag、GitHub Release，也不改变 `main`。

## Phase 1：非阻塞后台核心

- 主窗口先显示，启动安全检查随后通过 Coordinator、Worker 和 QThread 异步执行。
- 启动状态明确区分 `BOOTSTRAPPING`、`SAFETY_CHECKING`、`READY` 和可诊断只读降级。
- 后台任务具有唯一终态、协作取消、迟到结果保护和确定性清理；GUI 线程不调用 `wait()`，也不使用
  `QThread.terminate()`。
- 窗口关闭会拒绝仍不安全的路径，等待可取消任务稳定和移除后再重试，不留下活动任务或 QThread。

## Phase 2：协调合同与安全操作

- 采集服务、截图归档、索引刷新/清理、完整备份、下载引擎更新、结果刷新和队列推进统一进入后台协调。
- 每项任务声明资源冲突、去重键、取消策略和关闭策略；取消只允许发生在关键写入前。
- `CANCELLABLE -> CRITICAL_TO_COMPLETION` 是原子单向转换；关键写入开始后必须完成或明确失败。
- generation、进度节流、定向刷新和粘性 `CLOSING` 防止旧结果、重复信号或关闭期间 UI 更新污染当前状态。
- 下载汇总、截图/索引后处理、备份与引擎更新继续遵守正式数据、原子替换和唯一 Volume 安全边界。

## Phase 3：独立只读结果看板

- 结果看板只读取既有 `DownloadTask_*.log`，不写日志，不创建第二套结果数据库或正式持久缓存。
- 轻量任务索引支持默认选择、明确切换、自动加载和手动强制刷新。
- 进程内缓存按任务日志指纹失效；后台解析拥有独立 generation，快速切换、取消、窗口关闭和迟到信号
  都不能覆盖较新的选择。
- 看板显示任务状态、账号分布、完整性、前置异常、未开始、无法归类和来源证据；可打开任务日志及其
  实际引用的下载器原始日志。
- 账号列表固定高度并使用虚拟模型，支持关注项过滤、精确 A 编号查询和框内滚动；千级账号不会把页面撑高。
- 旧任务缺少字段时保留未知，不截断、不猜测；旧下载结果页、六类状态算法、账号映射和汇总顺序不变。

## 最终稳定化修复

### STAB-01：大账号智能跳过预览

- 预览窗口限制在当前屏幕可用区域内并可调整大小。
- 全部账号明细放入单一只读文本区域，支持鼠标滚轮和滚动条，不为每个账号创建 QWidget。
- “按预览跳过”“强制包含全部”“取消创建”始终固定可见。
- 标题栏关闭和 Esc 等价于取消；完整明细不截断、不丢失。
- 1370 个合成账号已完成自动和 Windows 前台验证；智能跳过算法、有效期、日志源、分类、账号映射、
  `skipped_numbers` 和模板生成语义均未改变。

### STAB-02：主窗口几何保存与恢复

- 首次启动按当前屏幕可用工作区使用安全且实用的默认尺寸。
- 最终正常接受关闭后保存普通窗口尺寸、位置和最大化状态；下次启动恢复。
- 损坏值、屏外位置、分辨率变化、显示器移除和 Windows 原生窗口外框均会约束到当前可用屏幕。
- 下载、汇总、监听、后台任务或采集停止失败导致的拒绝关闭不会保存新状态；异步关闭重试只保存一次。
- UI 状态使用独立、版本化的 UserScope INI 边界；不写入产品 Git、`settings_master.json`、正式
  `settings.json`、下载器数据库或业务配置。测试和验收使用独立注入路径。

## 验收与回归

- 稳定化专项测试覆盖 1370 账号完整文本、滚动、固定按钮、三种选择、关闭/Esc、窗口状态容错和全部拒绝关闭路径。
- Phase 1 和 Phase 2 Qt 生命周期门禁各完成 20/20；Phase 3 与稳定化生命周期验证无残留活动任务或 QThread。
- 最终候选全量回归：422 项通过，2 项冻结基线既有 Windows 条件测试跳过，0 失败。
- 普通非管理员 Windows 完整验收：14 个可见步骤全部 PASS，83/83 自动检查通过，应用/工具退出码 0，
  `active_tasks=false`，`qthreads_running=0`，未访问正式数据或启动外部下载器/采集器。

## 版本身份与 Artifact

- `pyproject.toml` 和 `douk_manager.__version__`：`0.1.5`。
- Windows `BUILD-INFO.txt`：`Version: 0.1.5`。
- Artifact：`DouK-Manager_Windows_X64-v0.1.5-<ref>-run-<number>-<sha>`。
- `develop` 推送触发 `Develop CI`；Test 通过后才运行 Windows portable package。

## 安全边界与升级

- v0.1.5 不迁移、不覆盖正式 `_internal\Volume`、数据库、settings、账号日志或原始下载器日志。
- 主档 `enable`、账号 A 编号映射、六类下载状态、汇总顺序和旧下载结果页合同保持不变。
- 从 v0.1.4 升级只替换管理器程序文件；保留正式数据和下载器目录。首次启动会在独立 UI 状态边界创建
  窗口几何记录，不影响业务配置。
- 如需回滚，先停止管理器、采集器和下载引擎，再恢复上一版管理器便携包；不回滚或迁移 Volume/数据库。

## 本阶段未执行

- 不创建 v0.1.5 Tag 或 GitHub Release。
- 不合并或推送 Phase 1、Phase 2、Phase 3、stabilization 技术分支。
- 不改变 `main`；最终历史通过 `feature/v0.1.5-final`、`develop` 和已验证的 v2 恢复 bundle 保留。
