# v0.1.0 功能与边界

## 已实现

- 固定识别当前 `main.exe` 与唯一正式 `_internal\Volume`；
- 每次打开前仅备份 settings_master.json、settings.json、DouK-Downloader.db，最多保留 3 份；
- SQLite Backup API 与 `PRAGMA quick_check`；
- 主档/任务JSON原子写入；
- `A1,A10-A99,A879`、中文逗号和混合范围；
- 越界、倒序和非法输入阻止；
- 主档 `enable` 不变的自动测试；
- 任务版选中账号 true、其他账号 false；
- 主档和任务版 `earliest` 的独立控制；
- `run_command = 5 1 1 Q`；
- 按250或任意数量自动拆分任务；
- 单任务、手动激活和顺序队列；
- 队列使用明确的位置控制及 `Alt+↑` / `Alt+↓` 移动，列表完全关闭拖放；
- 支持指定位置移动、按 A 编号恢复和重启后顺序持久化；
- 新任务按首个 A 编号插入人工槽位，已删除模板的旧顺序记录自动清除；
- “设为正式 settings.json”严格限制为只勾选一个模板；
- 任务前关键文件备份（自动限制保留数量）；
- 原采集器 API、固定令牌和油猴脚本兼容；
- 采集器与管理器跨进程文件锁；
- 旧Excel、分类和截图的只复制迁移；
- 旧Excel尾部自动补齐到唯一主档，历史链接写法差异原样保留；
- 迁移过程永不复制第二份主档；
- 截图双重预检、SHA-256校验和不覆盖移动；
- 刷新索引与清理失效快捷方式；
- 每批完成或整个队列完成后执行后处理；
- Windows X64 GitHub Actions 便携版构建；
- 源码仓库敏感数据阻止规则。

任务模板同名时自动追加编号，旧模板永不覆盖。程序运行、失败、下载启动、
退出码和后处理均写入 `Logs`；下载引擎本身的原生日志保持不变。退出码 `0`
只标记下载器进程正常结束，不会被显示为“所有账号下载成功”；账号级获取、跳过与
下载结果仍以下载器窗口和原生日志为准。每个下载器进程单独生成一份
`DownloadTask` 日志，其中会区分任务模板、实际所选账号数、暂停间隔账号数与
暂停秒数，不再用 `Batch accounts` 混淆任务账号数量。

## 当前下载引擎限制

当前 `20260626` EXE 的 50/150 仍为源码硬编码。管理器已经会传入：

```text
DOUK_ACCOUNT_BATCH_SIZE
DOUK_ACCOUNT_REST_SECONDS
```

但只有后续兼容补丁版下载引擎会读取这两个参数。在补丁版完成前，界面会明确提示当前EXE仍按固定50/150执行。

## 上线顺序

1. GitHub Actions 构建通过；
2. 在测试目录运行；
3. 只读识别正式路径；
4. 检查 Startup 关键文件备份及最多 3 份的保留限制；
5. 迁移采集器非主档数据；
6. 用少量账号创建模板；
7. 手动核对生成JSON；
8. 才允许首次正式下载。
