# DouK 全流程一体化管理器 v0.1.1

发布日期：待发布

## 版本定位

v0.1.1 是基于 v0.1.0 的兼容性修复版本。v0.1.0 Tag 和 Release 保持不变；本版本不重写既有发布历史。

## 修复内容

- 下载引擎运行期间允许启动和停止账号采集服务；
- 采集器继续使用管理器共享的短时文件锁处理预览、账号写入、分类和截图请求；
- 采集写入仍只涉及 `settings_master.json`、`录制名单.xlsx`、分类文件、截图及采集器自己的备份状态；
- 当前正式 `settings.json` 和 `DouK-Downloader.db` 不由采集器修改；
- 下载已经启动后，新采集账号只进入主档，不会加入该次运行使用的 `settings.json`；旧任务以后再次激活时，新账号默认禁用。
- 下载引擎启动在共享跨进程锁内执行二次运行检查、备份和进程创建；Windows 下载进程继承按引擎绝对路径生成的命名互斥句柄，在完整任务生命周期内阻止其他管理器实例启动第二个下载器。

## 保持不变的安全限制

下载引擎运行期间仍禁止：

- 迁移旧采集器数据；
- 切换正式路径或重建服务；
- 创建、激活或切换下载任务；
- 启动第二个下载引擎；
- 重新制作完整 Volume 备份；
- 更新或替换下载引擎。

v0.1.1 仍不提供图形化备份恢复入口。恢复只允许在下载器停止后按既有隔离验收流程进行。

## 测试边界

- 自动测试全部使用临时目录、脱敏 JSON、临时 Excel 和临时 SQLite 数据库；
- 锁冲突测试使用临时子进程占用临时锁文件，验证采集 POST 返回 `MANAGER_BUSY` 且四类临时数据不变；
- 未读取、复制或修改正式 `_internal\Volume`；
- 未修改 `loner913/TikTokDownloader`；
- 未增加联网自动更新、安装器、注册表写入或本机 Python 环境修改。

## 发布文件

GitHub Actions 构建通过后，将生成新的 Windows 便携版压缩包。发布时应新建 `v0.1.1` Tag 和 Release，不得移动或覆盖 `v0.1.0`。
