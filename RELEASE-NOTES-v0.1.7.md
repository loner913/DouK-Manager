# DouK 全流程一体化管理器 v0.1.7

## 版本定位

v0.1.7 在 v0.1.6 的账号、日志和引擎安全基础上，完成账号主页直达、独立观察名单、
Startup 六文件保护、统一主题界面和完整使用手册，重点减少日常管理步骤并保护正式账号数据边界。

当前状态为本地候选。各功能项 C/D、固定部署环境的全程序 UI 排查和已发现问题修复已经完成；
源码、运行时和 Windows 构建身份已统一为 `0.1.7`，当前身份候选的完整隔离回归和
普通 Windows 前台版本确认已经通过。版本级 E 尚未完成，因此本文不表示已经 push、创建 PR、生成正式包、
创建 Tag 或发布 GitHub Release。

当前本地最终分支：`feature/v0.1.7-final`。该分支由已验收的
`feature/v0.1.7-homepage` 候选原地重命名，没有重新合并已经集成的旧功能分支；当前尚未推送到 GitHub。

## 功能范围

### 账号主页直达

- 下载结果、结果看板和账号审计通过正式主档解析当前账号主页；
- 观察名单使用独立 W 记录中的受控地址；
- 地址通过安全校验后交给 Windows 默认浏览器，不写入日志或诊断导出；
- 保留下载结果表格双击打开来源日志的既有行为，主页使用独立操作入口。

### 独立观察名单与正式转正

- W 身份独立于正式 A 编号，不进入正式任务或改变正式主档 `enable`；
- W 高水位在归档、删除、恢复和迁移后都不降低、不复用；
- 支持复查时间、继续观察、备注、归档、恢复和二次确认永久删除；
- 转正复用正式采集的 JSON + Excel 事务，并保留可恢复的两阶段状态，避免不确定结果下重复追加；
- 油猴脚本提供“加入观察”，使用同一 `request_id` 进行有界重试，与正式采集入口保持隔离。

### Startup 六文件保护

- Startup 快照同时保护正式 `settings_master.json`、`settings.json`、`DouK-Downloader.db`
  和观察 `watchlist.json`、`watchlist_w_watermark.json`、`watchlist_control.json`；
- 快照记录 manifest、大小、SHA-256 与 SQLite quick check，最多保留 20 份有效快照；
- 首次可信初始化、写闸门、联合对账、隔离无效快照和只读恢复入口共同保护观察数据；
- 恢复观察正文时合并不可回退的 W 高水位和控制收据，不附带回滚正式 Volume。

### DouK Fluent Dashboard

- 保留 Python 3.12 + PySide6/Qt 和既有业务语义，统一 11 页导航、工具区、表格、状态和操作布局；
- 支持 `system`、`light`、`dark` 三种主题策略，切换立即生效并保存；
- 统一总览状态、观察提醒、账号任务、账号审计、下载队列、下载结果和观察复查等页面的视觉与交互；
- 固定部署环境为 27 英寸、2560×1440、Windows 100%；多缩放实机矩阵按 `V017-DEC-004` 取消。

### 使用手册与问题治理

- `docs/USER-MANUAL.md` 记录界面操作、数据影响、安全边界、失败表现和恢复方法；
- `docs/designs/v0.1.7/UI-ISSUES.md` 记录本轮 UI 修复和明确延后的优化项；
- 顶部搜索当前只支持页面/功能导航。账号与任务搜索或占位文案收窄登记为
  `V017-OPT-SEARCH-001`，延后到后续版本，不阻塞 v0.1.7。

## 验收与回归

- 各主页、观察、备份、采集、主题、页面流程和结果刷新功能均已有隔离 C 与普通 Windows 前台 D 证据；
- 固定部署环境的全程序逐页 UI/功能排查已完成，用户发现的问题已修复并接受；
- 版本身份专项为 `5 passed / 0 failed`；
- 当前身份候选的完整隔离回归为 `722 total / 720 passed / 2 skipped / 0 failed / 0 errors`；
- `compileall`、文档结构/链接、冲突标记、敏感文本边界和 `git diff --check` 均通过；
- 普通非管理员 Windows 前台确认源码候选显示 V0.1.7、Startup 到达 `READY`、正常关闭且 exit 0；
- 前台步骤使用隔离合成 home，受保护的观察/正式数据、Excel 和结果日志均未变化；
- 自动测试、离屏检查或静态检查不能替代最终前台整体验收。

## 版本身份与 Artifact

- `pyproject.toml` 和 `douk_manager.__version__`：`0.1.7`；
- Windows `BUILD-INFO.txt` 模板：`Version: 0.1.7`；
- Artifact：`DouK-Manager_Windows_X64-v0.1.7-<ref>-run-<number>-<sha>`；
- Artifact 同时包含 ZIP 和 `DouK-Manager_Windows_X64.zip.sha256`；
- 本地身份收口不生成 Artifact，正式包只能由后续获准的版本级 E 流程产生。

## 安全边界与升级

- 不读取、迁移或覆盖正式账号、配置、日志、数据库、Cookie、Token 或下载内容作为测试证据；
- 观察名单与正式账号保持分离，转正只能通过受控共享事务完成；
- 便携版验收应解压到隔离目录，不直接覆盖旧程序或正式运行目录；
- 程序文件回退不能撤销新版本已经产生的业务写入，正式数据恢复须按对应备份单独判断；
- 既有 v0.1.6 提交、文档、集成记录、Tag、Release 和 `main` 历史均不改写。
