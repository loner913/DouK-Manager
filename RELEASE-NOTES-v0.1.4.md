# DouK-Manager V0.1.4

## 任务模板与队列

- 下载队列支持全选、取消全选、鼠标左键拖动选中/取消整段模板和批量删除。
- 删除只作用于 `Data/Tasks/*.json` 模板，不删除排队任务、正式 `settings.json` 或历史 `DownloadTask_*.log`。
- 全选、取消全选和删除按钮位于队列顺序控制区域；所有队列按钮提供鼠标悬停说明。
- 下载队列提供任务目录、管理器日志目录和下载器原生日志目录入口。
- 队列暂停只阻止下一个模板启动，不暂停当前已经运行的下载进程。
- 下载队列显示并记录本次批量下载总耗时和当前任务耗时；完成、取消或异常中止时，耗时会写入实时信息与管理器日志。
- “取消全部下载任务”可结束当前由管理器启动的批量下载并清空本次尚未开始的模板；不会删除任务模板、
  历史日志、正式 `settings.json` 或数据库，也不会继续执行截图、索引和关机。
- 结果查看、暂停队列和完成后关机的影响关系在界面提示中明确说明；两个选项可以同时勾选，
  结果查看确认后才会进入关机倒计时。
- 结果查看采用每次运行独立的动态控制状态：未勾选时下载结束不会出现按键提示；运行期间取消勾选
  会让本次黑框自动关闭，但不会擅自恢复已经暂停的队列。
- 可选“全部任务完成后正常关机”：结果汇总、后续动作和日志都成功后进入 60 秒倒计时；采集服务运行/启用时不可用。

## 后台剪贴板监听

下载队列新增“启动后台监听”和“停止后台监听”。管理器启动时已有关键文件备份，
监听启动不再重复创建一份正式设置备份，只原子设置正式 `settings.json` 的 `run_command` 为 `6`；
启动时保留下载器的真实 Windows 控制台输入，避免监听窗口启动后立即退出。停止时先使用下载引擎公开支持的
剪贴板 `close` 指令并等待 2.5 秒，让监听器正常返回空闲主菜单；如果主菜单仍未自行退出，只关闭本管理器
启动的下载器进程树。确认进程退出后才恢复批量账号模式的 `5 1 1 Q`。用户直接关闭监听黑框时，管理器会
自动检测退出并恢复设置；两种模式共用一个下载引擎实例。

## 结果追溯与智能跳过

- 新增“下载结果”页面，按状态、A 编号、任务名称自动筛选现有任务日志；双击来源日志路径可直接打开日志。
- 下载结果页提供“立即刷新结果”，并在切换页面或账号汇总完成后自动刷新。
- V0.1.4 追加完整的账号主状态映射到原有 `DownloadTask_*.log`；旧日志不猜测缺失的正常账号。
- 下载引擎黑框被手动关闭或异常退出后，管理器仍会等待原生日志写入，并保留能够可靠归属的部分账号结果。
- 部分结果中明确的私密账号状态可供下载结果页和智能跳过使用；未开始、处理中断和未知状态不作为智能跳过依据。
- 账号任务页可开启“智能跳过近期已确认私密账号”，参考期限为 1–3650 天（默认关闭、默认 3 天）。
  预览显示输入、纳入、近期私密、已过期和无记录账号的完整分类，创建前提供“按预览跳过 /
  强制包含全部 / 取消创建”。
- 智能跳过扫描有效期内全部可解析任务日志，不受下载结果页面显示数量限制；较新的“未开始”记录不会
  擦除仍在有效期内的明确私密证据，后续明确正常结果则可让账号重新纳入。

## 全局状态与目录入口

- 所有主要页面右上角持续显示采集服务和下载进程状态，标题保持固定颜色，状态文字按运行情况变色。
- 下载队列可直接打开任务目录、管理器日志目录和下载器原生日志目录。
- 下载结果页修改状态、A 编号或任务名称后自动刷新，双击来源日志可直接打开证据文件。

## 关机安全说明

管理器倒计时结束后调用 Windows `shutdown.exe /s /t 0`，不带 `/f`，因此不会强制杀掉浏览器、QQ
等其他应用；未保存的应用可以阻止或延迟系统关机。非 Windows 环境不会执行关机。

## 2026-08-14 最终验收记录

最终运行时代码提交 `0ae2dca9ab6ba4fc8aa42f1aaf63fb39b3c6226e` 已完成 Windows 实机验收：

- 自动测试共 202 项，200 通过，2 项可选 WScript 桌面测试跳过，0 失败；
- 模板拖选/反向取消、全选/取消全选、限定范围删除和队列排序通过；
- 下载结果筛选、来源日志打开、有效期内全部日志私密证据和强制包含通过；
- 后台监听启动、剪贴板接收、停止、重复启停及 `6` 与 `5 1 1 Q` 安全切换通过；
- 结果查看关闭/开启/运行中动态取消与暂停队列的组合通过；
- 单模板和多模板“取消全部下载任务”通过；
- 采集服务关机安全门、60 秒倒计时与取消关机通过；
- 正式 Volume、数据库、settings、日志和备份均未进入源码变更。

## 已知边界与回滚

- 队列暂停只控制下一个模板，不是下载引擎内部断点暂停；异常退出后的断点续传不在 v0.1.4 范围内。
- 智能跳过是有效期内日志证据辅助，账号状态可能变化，创建前仍应查看预览并可选择强制包含。
- 如需回滚，先停止管理器、采集器和下载引擎，再恢复上一版管理器便携包；不迁移、不覆盖正式 Volume。
- v0.1.4 仍是 `develop` 开发版本，不移动或覆盖既有 `v0.1.0` Tag/Release。
