# DouK-Manager V0.1.4

## 任务模板与队列

- 下载队列支持全选、取消全选、鼠标左键拖选（Qt ExtendedSelection 会将拖选行勾选）和批量删除。
- 删除只作用于 `Data/Tasks/*.json` 模板，不删除排队任务、正式 `settings.json` 或历史 `DownloadTask_*.log`。
- 下载队列提供任务目录、管理器日志目录和下载器原生日志目录入口。
- 队列暂停只阻止下一个模板启动，不暂停当前已经运行的下载进程。
- 可选“全部任务完成后正常关机”：结果汇总、后续动作和日志都成功后进入 60 秒倒计时；采集服务运行/启用时不可用。

## 后台剪贴板监听

下载队列新增“启动后台监听”和“停止后台监听”。启动前备份并原子设置正式
`settings.json` 的 `run_command` 为 `6`，停止并确认引擎退出后恢复为批量账号模式的
`5 1 1 Q`。两种模式共用一个下载引擎实例；异常退出后管理器启动时会修复残留的 `6`。

## 结果追溯与智能跳过

- 新增“下载结果”页面，按状态、A 编号、任务名称筛选现有任务日志。
- V0.1.4 追加完整的账号主状态映射到原有 `DownloadTask_*.log`；旧日志不猜测缺失的正常账号。
- 账号任务页可开启“智能跳过近期已确认私密账号”，参考期限为 1–3650 天（默认关闭、默认 3 天）。
  创建前显示来源和跳过名单，提供“按预览跳过 / 强制包含全部 / 取消创建”。

## 关机安全说明

管理器倒计时结束后调用 Windows `shutdown.exe /s /t 0`，不带 `/f`，因此不会强制杀掉浏览器、QQ
等其他应用；未保存的应用可以阻止或延迟系统关机。非 Windows 环境不会执行关机。
