# DouK 全流程一体化管理器

面向 Windows X64 的便携式自用管理程序，统一管理：

- DouK 账号采集器本机服务；
- `settings_master.json` 与任务版 `settings.json`；
- `A1,A10-A99,A879` 形式的账号选择；
- 固定数量或自定义范围的批次任务；
- DouK-Downloader 下载引擎调用；
- 账号截图安全归档；
- 视频账号文件夹快捷方式索引；刷新和独立清理都会按类别显示发现、计划、删除、失败与剩余数量；
- 启动前、修改前和下载前只备份 3 个关键文件并自动限制保留数量；下载引擎更新前完整备份 Volume；
- 管理器日志和下载任务日志。
- 下载引擎 ZIP 只读预检、安全更新和旧引擎程序永久留存；更新时移动保留唯一正式 Volume。
- 独立结果看板、后台任务生命周期协调，以及可恢复且屏幕安全的主窗口布局。

## 版本历史

仓库使用 `v0.1.0` 表示首个正式稳定版；后续 `v0.1.1` 至 `v0.1.5` 在
`develop` 上持续验收。当前 `v0.1.6` 已完成阶段 D 验收，以及 E-0 版本元数据和构建流程准备；
发布候选由 `feature/v0.1.6-final` 承载。实际发布状态以仓库的 `v0.1.6` Tag 和 GitHub Release 为准。
既有 `v0.1.0` Tag/Release 和 v0.1.5 历史保持不变。

| 版本 | 定位 | 主要新增与修复 |
| --- | --- | --- |
| [v0.1.0](RELEASE-NOTES-v0.1.0.md) | 首个 Windows 实机闭环稳定版 | 账号采集、任务模板与顺序队列、`5 1 1 Q` 下载调用、截图归档、快捷方式索引、关键文件备份和下载引擎安全更新 |
| [v0.1.1](RELEASE-NOTES-v0.1.1.md) | 并发与兼容性修复 | 允许下载期间启停采集服务；增加共享文件锁、跨进程下载器互斥和二次运行检查 |
| [v0.1.2](RELEASE-NOTES-v0.1.2.md) | 日志目录整理 | 将管理器、采集器、下载任务、索引刷新和索引清理日志归入五个英文分类目录；旧日志不自动迁移 |
| [v0.1.3](RELEASE-NOTES-v0.1.3.md) | 下载结果汇总 | 区分进程退出与账号结果，汇总下载、跳过、无作品、私密、异常、中断和未开始状态并追加到任务日志 |
| [v0.1.4](RELEASE-NOTES-v0.1.4.md) | 队列控制、监听与结果追溯 | 模板批量选择/删除、暂停与取消队列、后台剪贴板监听、下载结果页、私密账号智能跳过、动态结果查看、完成后安全关机和全局运行状态 |
| [v0.1.5](RELEASE-NOTES-v0.1.5.md) | 后台安全、结果看板与稳定化 | 三阶段后台生命周期与只读结果看板；大账号智能跳过预览可滚动；主窗口安全记忆尺寸、位置和最大化状态 |
| [v0.1.6](RELEASE-NOTES-v0.1.6.md) | 账号、日志与引擎安全 | mark 归类修复、日志安全统计与脱敏、下载引擎回退、账号健康审计与生命周期管理 |

完整的逐版本新增、修复、安全边界和升级说明见 [CHANGELOG.md](CHANGELOG.md)。

## 下载引擎提速阶段状态

DouK-Manager 当前只配套下载引擎已经验收并冻结的两个定制提速阶段：

- 第一阶段：标准账号长链接优先本地正则解析；未命中、不确定、异常或短链接回退原 GET。
  [固定分支](https://github.com/loner913/TikTokDownloader/tree/perf/stage-01-user-link-shortcut)
  · [固定提交](https://github.com/loner913/TikTokDownloader/commit/0b88ed875558064be026952fb98cd2b3174a4047)
- 第二阶段：固定每批 20 个账号的 Info 批量预取，异常时回退原单账号 Info，并保持原有
  `50/30` 暂停机制。
  [固定分支](https://github.com/loner913/TikTokDownloader/tree/perf/stage-02-info-batch)
  · [固定提交](https://github.com/loner913/TikTokDownloader/commit/34ae30fb6c4c2f6468ee777e649e3d20277280a8)

下载引擎提速研发在第二阶段批量 Info 完成后正式结束。第三、第四阶段及随机等待均值配置
不属于当前产品或候选包；阶段细节和验收边界见
[引擎提速阶段收尾记录](https://github.com/loner913/TikTokDownloader/blob/docs/performance-stage-closeout/docs/performance-stages.md)。

## v0.1.6 账号、日志与引擎安全

v0.1.6 在已验收的 v0.1.5 基础上整合四项功能：

- 35 项 `mark` 归类修复，保留原始值并拒绝歧义别名；
- 下载引擎回退的安全预检、备份点和失败恢复；
- 日志安全统计、全量脱敏和诊断导出边界；
- 账号健康审计、重复/冲突复核、永久停用和生命周期管理。

阶段 D 的最终候选已完成 Windows 前台人工验收。完整回归记录为 608 项测试，606 项通过、2 项条件跳过、0 失败、0 错误。
E-0 版本元数据和构建流程准备已完成；发布候选由 `feature/v0.1.6-final` 承载。实际发布状态以仓库的
`v0.1.6` Tag 和 GitHub Release 为准。

## v0.1.5 后台安全、结果看板与稳定化

v0.1.5 在 v0.1.4 既有账号任务、下载队列、智能跳过和下载结果页之上，按三个冻结阶段完成
后台生命周期和结果看板，并在最终稳定化阶段修复两个影响日常使用的问题。三个阶段保持线性继承，
不会覆盖或改写前一阶段的验收历史。

### Phase 1：非阻塞后台核心

- 主窗口先显示，再异步执行启动安全检查；`BOOTSTRAPPING`、`SAFETY_CHECKING`、`READY` 和只读降级状态可见。
- 统一 Coordinator、Worker 和 QThread 生命周期，耗时工作不阻塞 Qt 主线程；任务只产生一次终态并在
  `thread.finished` 后完成清理。
- 关闭窗口时采用协作取消和 idle 后重试，不在 GUI 线程等待或强制终止 QThread；迟到结果不能更新已关闭窗口。
- 缺失路径、进程探针不确定和启动检查失败进入可诊断的只读状态，不会静默放行危险操作。

### Phase 2：共享协调、安全合同与后台操作

- 采集服务、截图归档、索引维护、完整备份、下载引擎更新、结果刷新和队列推进接入统一后台协调。
- 每项任务声明真实资源冲突、去重标识、取消策略和关闭策略；`CANCELLABLE` 只能单向进入
  `CRITICAL_TO_COMPLETION`，关键写入开始后不再伪装成可取消。
- generation 过滤、进度节流、粘性 `CLOSING` 和定向刷新阻止旧任务、重复信号或无关页面刷新覆盖新状态。
- 下载汇总和后处理继续遵守既有顺序；诊断接口保持只读，正式 JSON 仍使用校验和原子替换。

### Phase 3：独立只读结果看板

- 新增独立“结果看板”，唯一数据源仍是既有 `DownloadTask_*.log`；不创建第二套数据库或正式持久缓存。
- 支持任务索引、明确选择、自动加载、手动强制刷新、进程内指纹缓存和 generation 过期结果丢弃。
- 看板显示任务状态、分布、完整性、未知原因与证据来源；可打开任务日志和实际引用的下载器原始日志。
- 账号明细使用固定高度的虚拟列表，支持关注项过滤、精确 A 编号查询和框内滚动，千级账号不会生成
  每账号独立控件或把页面无限撑高。
- 旧任务缺少新字段时保持未知，不猜测、不截断；旧“下载结果”页、六类状态算法、汇总顺序和账号映射不变。

### 最终稳定化

1. **大账号智能跳过预览**：预览改为不超过当前屏幕可用区域的可调整模态窗口；完整明细位于单一只读
   滚动区域，滚轮和滚动条可用，三个操作按钮固定可见。1370 个合成账号已验证；按预览跳过、强制包含全部、
   取消、关闭和 Esc 的既有业务语义不变。
2. **主窗口几何恢复**：首次启动使用当前屏幕约 90% 的安全尺寸；正常接受关闭后保存普通尺寸、位置和
   最大化状态。损坏记录、显示器移除、分辨率变化和屏外原生窗口外框会回退到当前可用屏幕；下载、汇总、
   后台任务或采集停止失败导致的拒绝关闭不会错误保存新状态。UI 状态独立于正式 settings、数据库和产品 Git。

最终稳定化候选完成 422 项全量测试（另有 2 项冻结基线既有 Windows 条件跳过），并通过普通非管理员
Windows 前台完整验收：14 个可见步骤和 83 项自动检查全部通过，退出码为 0，最终无活动任务或 QThread。

## v0.1.4 队列、监听和结果追溯

下载队列支持模板全选/取消全选、鼠标左键拖动选中或取消整段后批量删除，删除范围严格限定为
`Data/Tasks/*.json`；任务页提供打开任务目录和日志目录入口。新增后台剪贴板监听按钮：
监听使用 `run_command=6`，批量账号下载使用现有 `5 1 1 Q`，停止监听或启动时发现残留会安全恢复；
监听启动不重复生成正式设置备份，管理器启动时的关键文件备份继续作为统一安全快照。

“下载结果”页从现有 `DownloadTask_*.log` 读取账号状态，筛选会自动刷新，双击来源日志可直接打开；账号任务创建页可按最近 N 天
（默认 3 天、可改为 7/15 等）预览并智能跳过明确私密账号，支持强制包含全部。队列暂停只暂停
队列推进，不影响当前下载；完成后关机默认关闭，只有结果可靠且采集服务未启用时才提供 60 秒可取消的
Windows 正常关机倒计时。

下载结果页支持立即刷新，并会在切换页面或账号汇总完成后更新。同一管理程序会话中，即使下载引擎黑框
被手动关闭或异常退出，也会保留能够从原生日志可靠确认的部分账号结果；明确的私密账号结果可继续作为
智能跳过依据，未开始、处理中断和未知状态不会触发跳过。下载队列同时显示并记录本次批量下载总耗时和
当前任务耗时。

“取消全部下载任务”会停止当前由管理器启动的批量下载并清空本次待执行模板，不删除模板、日志、
`settings.json` 或数据库；结果查看在任务运行期间动态生效，未勾选时不会出现结束后按键提示。

下载器退出码 `0` 只代表进程正常结束。账号可能因接口错误、私密状态、没有新作品
或防重复记录而没有产生下载结果，管理器不会把进程正常退出误报为账号下载成功。

## v0.1.3 下载账号结果汇总

每个下载进程结束后，管理器会在启动下一项队列任务前逐行分析本次下载器原生日志，
分别显示进程退出状态与账号结果。汇总包含计划及实际开始账号数、各主状态数量，
以及私密、异常、中断、未开始和异常后完成账号的完整 A 编号；证据不足时会明确标记
“结果不完整”或“无法可靠汇总”。原生日志保留在下载器原路径，不复制、不修改；
除既有 `DownloadTask_*.log` 追加汇总外，不生成独立缓存或结果文件。六类已开始账号
主状态互斥且合计等于实际开始数；“完成但有异常记录”是可与主状态重叠的附加状态，
明确不计入主状态合计。

## 任务模板怎样复用

- `F:\DouK-Manager\Data\Tasks\*.json` 是永久保留、可反复使用的完整任务模板；
- 下载器不会直接读取 `Data\Tasks`，它始终只读取唯一正式
  `_internal\Volume\settings.json`；
- “将勾选任务设为正式 settings.json”会先备份 3 个关键文件，再把一个模板复制为正式文件；
- “按顺序运行勾选任务”会逐个执行“备份 → 激活为正式 settings.json → 启动下载”；
- 队列列表完全关闭拖放；高亮任务后可移到最前、上移、下移、最后或指定第 N 位；
- 高亮任务时可使用 `Alt+↑` / `Alt+↓` 快速上移或下移；
- 人工顺序保存在 `Data\task_order.json`，不修改模板内容；新模板仍按首个 A 编号插入人工槽位；
- “恢复按 A 编号排序”只在用户主动点击时清除人工槽位；
- “运行当前正式 settings.json”不使用任务列表，直接运行当时已经激活的正式文件；
- 下载结束后的黑框可以选择保留以查看统计，也可以在无人值守队列中选择自动关闭；结果查看会影响后续队列和关机倒计时，
  相关选项支持在任务运行期间动态生效。
- 每启动一次下载器进程生成一份 `DownloadTask` 日志；日志明确记录任务模板、
  实际启用且 URL 有效的账号数、每多少账号暂停一次及暂停秒数。

## 重要安全原则

1. 唯一正式数据始终位于下载引擎的 `_internal\Volume`。
2. `settings_master.json` 的 `enable` 不会因任务选择而改变。
3. 任务选择只在任务版 `settings.json` 中设置 `enable`。
4. 只有用户明确勾选时，才会把所选账号的 `earliest` 写回主档。
5. 所有正式 JSON 使用“临时文件 + 校验 + 原子替换”。
6. 修改正式数据或启动批量下载前必须先完成关键文件备份；后台监听沿用管理器启动时的关键快照，不重复备份；完整 Volume 备份只在手动确认或更新引擎前执行。
7. 更新下载引擎时禁止覆盖 `_internal\Volume`。
8. 本仓库禁止提交数据库、主档、任务配置、Cookie、截图、日志和备份。
9. 旧采集器 Excel 只补齐尾部缺失账号；既有行即使链接写法不同也原样保留。
10. 同名任务模板自动递增编号，不覆盖旧模板。
11. 索引功能只读取视频账号源目录；删除仅限快捷方式索引目录顶层带 `[DoukIndex]` 标记的 `.lnk`。

## 默认正式路径

```text
下载引擎：F:\DouK-Downloader_Custom_50_150\DouK-Downloader_Windows_X64_20260626\main.exe
视频目录：F:\DouK-Downloader
索引目录：F:\Douk videos
运行目录：F:\DouK-Manager
```

所有路径都可以在管理器设置页面修改。正式 `Volume` 路径由下载引擎路径推导，不能单独指向另一份主档。

## 账号表达式示例

```text
A1,A10-A99,A879
1,10-99,879
A1，A10-A99，A879
A1-A250
```

编号按 `accounts_urls` 的数组位置固定映射，不会因为空 URL 而重新编号。

## 本地源码运行

需要 Python 3.12：

```powershell
python -m pip install -e .
python main.py
```

运行自动测试：

```powershell
python -m unittest discover -s tests -v
```

## Windows 便携版构建

仓库包含 `.github/workflows/build-windows.yml`。V0.1.6 发布准备分支为
`feature/v0.1.6-final`。该分支推送后，在仓库 `Actions` 中手动选择同一工作流和该分支运行：

1. `Test` 作业先在 Windows + Python 3.12 上运行全量 unittest；
2. 测试通过后运行 `Windows portable package`；
3. 下载同时包含 ZIP 和 SHA-256 校验文件的 Artifact；
4. 核验 ZIP 后解压到隔离目录，再双击 `DouKManager.exe`。

Artifact 名称格式为 `DouK-Manager_Windows_X64-v0.1.6-<ref>-run-<run>-<sha>`。
编译产物、运行数据和正式账号资料都不会写入源码仓库。

## v0.1.0 稳定版

v0.1.0 已完成 Windows 实机闭环验收，覆盖账号采集、任务生成与队列、下载引擎调用、截图归档、索引刷新与受管快捷方式清理。

最终验收包括：

1. 两种“每批完成 / 整个队列完成”后处理组合；
2. `earliest` 仅任务生效及按需写回主档，并确认主档 `enable` 不变；
3. 单下载进程保护、重复启动拦截及异常退出后停止剩余队列；
4. 三个关键文件的副本恢复、哈希、JSON解析和SQLite检查；
5. 隔离索引中空源目录快捷方式、目标不存在快捷方式及正常快捷方式的正向删除边界。

管理器会通过环境变量向兼容版下载引擎传入“每批账号数 / 暂停秒数”。未包含兼容补丁的原版引擎会忽略这些变量并按自身内置规则运行。兼容版下载引擎仍可直接双击；未收到管理器参数时默认使用 `50/150`。

详细范围和已知边界见 [FEATURES-v0.1.0.md](FEATURES-v0.1.0.md) 与 [RELEASE-NOTES-v0.1.0.md](RELEASE-NOTES-v0.1.0.md)。
