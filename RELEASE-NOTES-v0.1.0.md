# DouK 全流程一体化管理器 v0.1.0

发布日期：2026-08-11

## 版本定位

v0.1.0 是首个完成 Windows 实机闭环验收的稳定版本。程序以统一控制中心整合账号采集、任务配置、下载引擎调用、截图归档和快捷方式索引，同时继续将下载器作为可独立运行的外部引擎。

## 主要能力

- 按需启停本机账号采集服务，并兼容既有油猴脚本；
- 使用唯一正式 `settings_master.json`，任务选号不改变主档 `enable`；
- 支持 `A1,A10-A99,A879`、固定数量分批及可复用任务模板；
- 单模板激活和多模板顺序队列，支持明确的位置移动及 `Alt+↑ / Alt+↓`；
- 通过 `5 1 1 Q` 调用下载引擎，并向兼容版传入批次数量和暂停秒数；
- 启动、修改和下载前仅备份三个关键文件，完整 Volume 备份只在手动确认或引擎更新前执行；
- 截图预检、安全归档及不覆盖移动；
- 增量刷新索引，分别统计空源目录和目标不存在的受管快捷方式；
- 删除范围严格限定为索引目录顶层受管 `.lnk`；视频源目录只读；
- 信息框按真实时间和事项分行显示，退出码0不会被误报为账号下载成功；
- 下载引擎ZIP预检、安全替换、唯一正式 Volume 保护和旧引擎回退。

## 最终验收结果

1. 两种“每批完成 / 整个队列完成”后处理组合均符合预期；
2. `earliest` 仅任务模式与写回主档模式均符合预期，主档 `enable` 差异为0；
3. 重复启动被拦截，全程只有一个下载器进程，异常退出不会启动剩余任务；
4. 三个关键文件在隔离副本中恢复后哈希一致，两个JSON可解析，数据库 `quick_check=ok`；
5. 隔离索引首次只删除空源目录和目标不存在对应的两个受管快捷方式，保留正常快捷方式和全部源目录；第二次删除0个。

## 已知边界

- 下载器进程退出码0只表示进程正常结束，账号级下载结果仍需结合原生窗口和日志；
- v0.1.0 尚无图形化备份恢复按钮，恢复演练使用隔离副本完成；
- v0.1.0 尚无“完成当前账号后停止”入口，异常退出会停止剩余队列；
- 未包含兼容补丁的原版下载引擎不会读取管理器传入的动态暂停参数；
- 项目不包含下载引擎、正式 Volume、账号主档、数据库、Excel、截图、Cookie、日志或备份。

## 发布文件

GitHub Release 附件应上传 GitHub Actions 生成的：

```text
DouK-Manager_Windows_X64.zip
```

运行时保留同一目录中的：

```text
DouKManager.exe
_internal
BUILD-INFO.txt
```
