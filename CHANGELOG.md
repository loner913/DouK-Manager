# DouK-Manager 版本历史

本文记录每个开发版本新增了什么、修复了什么，以及版本之间的继承关系。详细验收和安全边界请继续查看各版本的 `RELEASE-NOTES`。

## v0.1.4 — 队列控制、监听与结果追溯（2026-08-14）

状态：`develop` 开发版本，Windows 实机验收通过；不移动既有 `v0.1.0` Tag/Release。

### 新增

- 下载队列模板全选、取消全选、鼠标拖选/反向取消和限定范围批量删除；
- 任务目录、管理器日志目录和下载器原生日志目录快捷入口；
- 全局采集服务与下载进程状态；
- 后台剪贴板监听，安全切换 `run_command=6` 与批量模式 `5 1 1 Q`；
- 独立下载结果页面、自动筛选和来源日志追溯；
- 按 1–3650 天期限扫描全部可解析日志的私密账号智能跳过、预览和强制包含；
- 只暂停队列推进的暂停/恢复功能，以及停止当前进程并清空待执行模板的“取消全部下载任务”；
- 动态结果查看和采集服务安全门保护的 60 秒正常关机倒计时。

### 关键修复

- 修复监听进程标准输入被重定向后启动即退出；
- 修复停止监听返回主菜单后长时间不关闭，改为先 `close` 正常退出监听，再关闭管理器所属进程树；
- 修复结果查看未勾选仍无条件出现按键提示，以及运行中取消不生效；
- 修复智能跳过只受有限结果集合影响、较新“未开始”记录遮蔽有效私密证据；
- 修复拖选被 Qt 默认列表行为吞掉的问题，并支持再次拖过取消同一区间。

详细说明：[RELEASE-NOTES-v0.1.4.md](RELEASE-NOTES-v0.1.4.md)

## v0.1.3 — 下载账号结果汇总（2026-08-13）

状态：`develop` 开发构建，Windows 实机验收通过。

### 新增与修复

- 将下载器进程退出状态与账号下载结果分开判断，避免退出码 `0` 被误报为账号下载成功；
- 从本次原生日志区间汇总下载、全部跳过、无符合条件作品、私密、异常、中断和未开始状态；
- 将账号汇总追加到既有 `DownloadTask_*.log`，不创建第二套结果数据库或缓存；
- 用户停止和异常退出时保留已经可靠确认的账号结果，其余账号按中断或未开始分类；
- 队列必须等本次账号汇总完成后才能启动下一模板。

详细说明：[RELEASE-NOTES-v0.1.3.md](RELEASE-NOTES-v0.1.3.md)

## v0.1.2 — 日志目录整理（2026-08-12）

状态：`develop` 开发构建。

### 新增与修复

- 将新日志统一分类到 `Logs\Manager`、`Logs\Collector`、`Logs\DownloadTasks`、`Logs\IndexRefresh` 和 `Logs\IndexCleanup`；
- 索引脚本改为显式写入管理器日志区，不再向索引目录写入新日志；
- 保持旧日志只读，不自动扫描、迁移、重命名或删除。

详细说明：[RELEASE-NOTES-v0.1.2.md](RELEASE-NOTES-v0.1.2.md)

## v0.1.1 — 并发与兼容性修复

状态：功能已完成并纳入后续 `develop` 演进；未单独创建 Tag/Release。

### 新增与修复

- 允许下载引擎运行期间启停账号采集服务；
- 采集写入继续受共享短时文件锁保护，且不修改当前正式 `settings.json` 和下载数据库；
- 下载启动在共享跨进程锁内进行二次运行检查、备份和进程创建；
- Windows 下载进程持有按引擎绝对路径生成的命名互斥句柄，防止不同管理器实例启动第二个下载器；
- 保留路径迁移、任务切换、完整备份和引擎更新等高风险操作的运行期禁用规则。

详细说明：[RELEASE-NOTES-v0.1.1.md](RELEASE-NOTES-v0.1.1.md)

## v0.1.0 — 首个稳定版（2026-08-11）

状态：首个 Windows 实机闭环稳定版；仓库现有正式 Tag/Release。

### 新增

- 统一账号采集、账号任务、批次生成、顺序下载、截图归档和快捷方式索引；
- 支持 `A1,A10-A99,A879` 账号表达式、固定数量分批和永久任务模板；
- 通过 `5 1 1 Q` 调用下载引擎，并向兼容版传入账号批次数量和暂停秒数；
- 关键文件有限保留备份、手动完整 Volume 备份、下载引擎 ZIP 预检、安全更新和旧引擎回退；
- 下载器单实例保护、正式 Volume 唯一性、主档 `enable` 永不随任务选择改变等安全边界。

详细说明：[RELEASE-NOTES-v0.1.0.md](RELEASE-NOTES-v0.1.0.md)
